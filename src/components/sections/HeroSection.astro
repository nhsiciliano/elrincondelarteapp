<section id="hero" class="hero container">
  <div class="hero__content fade-up">
    <span class="tag">Estudio de Danza</span>
    <h1>Movimiento que transforma, técnica que inspira</h1>
    <p>
      Te acompañamos a construir tu identidad artística con clases personalizadas,
      procesos creativos y un seguimiento cercano en cada estilo.
    </p>
    <div class="hero__actions">
      <a class="btn" href="#horarios-2025">Ver Horarios 2025</a>
      <a class="hero__contact" href="#contacto">Agendar clase prueba</a>
    </div>
  </div>
  <div class="hero__media fade-up" style="animation-delay: 120ms">
    <div class="hero__image-wrapper">
      <img src="/images/hero-showcase.jpg" alt="Leila Palavecino guiando a bailarinas" />
      <div class="hero__badge">
        <strong>+10 años</strong>
        <span>formando artistas</span>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 3rem;
    align-items: center;
    padding-top: 128px;
  }

  .hero__content h1 {
    font-size: clamp(2.5rem, 7vw, 4rem);
    margin: 1rem 0;
    line-height: 1.05;
  }

  .hero__content p {
    margin: 0;
    color: var(--color-text-muted);
    font-size: 1.05rem;
    max-width: 540px;
  }

  .hero__actions {
    display: flex;
    gap: 1.25rem;
    flex-wrap: wrap;
    margin-top: 2rem;
    align-items: center;
  }

  .hero__contact {
    font-weight: 600;
    color: var(--color-accent-strong);
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    position: relative;
  }

  .hero__contact::after {
    content: '';
    position: absolute;
    inset: auto 0 -4px;
    height: 2px;
    background: var(--color-accent-strong);
    opacity: 0.5;
  }

  .hero__media {
    justify-self: end;
  }

  .hero__image-wrapper {
    position: relative;
    border-radius: var(--radius-lg);
    overflow: hidden;
    box-shadow: var(--shadow-soft);
  }

  .hero__image-wrapper img {
    width: min(100%, 540px);
    height: auto;
  }

  .hero__badge {
    position: absolute;
    bottom: 16px;
    right: 16px;
    padding: 0.85rem 1.1rem;
    border-radius: var(--radius-sm);
    background: rgba(255, 255, 255, 0.85);
    backdrop-filter: blur(18px);
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 0.15rem;
    color: var(--color-text);
    box-shadow: 0 20px 35px rgba(47, 42, 52, 0.12);
  }

  .hero__badge strong {
    font-size: 1.1rem;
    letter-spacing: 0.04em;
  }

  @media (max-width: 1024px) {
    .hero {
      grid-template-columns: 1fr;
      padding-top: 96px;
    }

    .hero__media {
      justify-self: stretch;
      order: -1;
    }

    .hero__image-wrapper img {
      width: 100%;
    }
  }

  @media (max-width: 680px) {
    .hero {
      gap: 2.5rem;
      padding-top: 72px;
    }

    .hero__actions {
      flex-direction: column;
      align-items: flex-start;
    }

    .hero__badge {
      position: static;
      margin-top: 1.25rem;
    }
  }
</style>
