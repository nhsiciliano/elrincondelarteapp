---
import { danceStyles } from '../../data/styles';
---

<section id="estilos" class="styles">
  <div class="container">
    <div class="section-heading fade-up">
      <span>Estilos</span>
      <h2>Explorá cada disciplina con procesos creativos personalizados</h2>
      <p>
        Clases reducidas, combinación de técnica, musicalidad y entrenamiento físico para acompañar tu
        evolución en cada etapa.
      </p>
    </div>
    <div class="styles__grid">
      {danceStyles.map((style, index) => (
        <article
          class={`styles__card card-surface fade-up ${index % 2 === 0 ? 'is-text-first' : 'is-image-first'}`}
          style={`animation-delay: ${index * 80}ms`}
        >
          <div class="styles__content">
            <span class="styles__index">{String(index + 1).padStart(2, '0')}</span>
            <h3>{style.name}</h3>
            <p>{style.description}</p>
          </div>
          <figure class="styles__image">
            <img src={style.imageSrc} alt={style.alt} loading="lazy" />
          </figure>
        </article>
      ))}
    </div>
  </div>
</section>

<style>
  .styles__grid {
    margin-top: 3.5rem;
    display: grid;
    gap: 2.25rem;
  }

  .styles__card {
    --style-primary: rgba(245, 155, 189, 0.5);
    --style-secondary: rgba(156, 218, 220, 0.35);
    position: relative;
    display: grid;
    grid-template-columns: minmax(0, 1.05fr) minmax(0, 0.95fr);
    align-items: stretch;
    overflow: hidden;
    min-height: 260px;
    padding: 2.1rem;
    border-radius: var(--radius-lg);
    background: linear-gradient(135deg, var(--style-primary), var(--style-secondary));
    box-shadow: 0 26px 46px rgba(47, 42, 52, 0.18);
    transform: translateY(0) scale(1);
    transition: transform var(--transition-base), box-shadow var(--transition-base);
  }

  .styles__card::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(160deg, rgba(255, 255, 255, 0.95), transparent 65%);
    mix-blend-mode: screen;
    opacity: 0.85;
    z-index: -1;
  }

  .styles__card:nth-child(1) {
    --style-primary: rgba(245, 155, 189, 0.7);
    --style-secondary: rgba(255, 218, 231, 0.9);
  }

  .styles__card:nth-child(2) {
    --style-primary: rgba(156, 218, 220, 0.65);
    --style-secondary: rgba(207, 233, 252, 0.9);
  }

  .styles__card:nth-child(3) {
    --style-primary: rgba(255, 189, 140, 0.7);
    --style-secondary: rgba(246, 215, 182, 0.9);
  }

  .styles__card:nth-child(4) {
    --style-primary: rgba(199, 180, 255, 0.65);
    --style-secondary: rgba(232, 209, 255, 0.88);
  }

  .styles__card:nth-child(5) {
    --style-primary: rgba(255, 197, 207, 0.75);
    --style-secondary: rgba(156, 218, 220, 0.55);
  }

  .styles__card.is-text-first .styles__content {
    order: 1;
  }

  .styles__card.is-text-first .styles__image {
    order: 2;
  }

  .styles__card.is-image-first .styles__content {
    order: 2;
  }

  .styles__card.is-image-first .styles__image {
    order: 1;
  }

  .styles__content {
    padding: 1.8rem;
    display: flex;
    flex-direction: column;
    gap: 0.85rem;
  }

  .styles__index {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 56px;
    height: 56px;
    border-radius: 18px;
    background: rgba(255, 255, 255, 0.78);
    font-weight: 600;
    font-size: 1.15rem;
    letter-spacing: 0.05em;
    color: var(--color-text);
    box-shadow: 0 16px 30px rgba(47, 42, 52, 0.18);
  }

  .styles__content h3 {
    margin: 0;
    font-size: 1.7rem;
    letter-spacing: -0.01em;
  }

  .styles__content p {
    margin: 0;
    color: var(--color-text-muted);
    font-size: 1rem;
    line-height: 1.55;
  }

  .styles__image {
    position: relative;
    height: 100%;
    border-radius: var(--radius-md);
    overflow: hidden;
  }

  .styles__image::after {
    content: '';
    position: absolute;
    inset: auto 0 0;
    height: 45%;
    background: linear-gradient(180deg, transparent, rgba(47, 42, 52, 0.45));
    opacity: 0;
    transition: opacity var(--transition-base);
  }

  .styles__image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transform: scale(1.02);
    transition: transform var(--transition-base);
  }

  .styles__card:hover {
    transform: translateY(-12px) scale(1.03);
    box-shadow: 0 46px 70px rgba(47, 42, 52, 0.22);
  }

  .styles__card:hover .styles__image::after {
    opacity: 1;
  }

  .styles__card:hover .styles__image img {
    transform: scale(1.08);
  }

  @media (max-width: 1024px) {
    .styles__card {
      grid-template-columns: 1fr;
      padding: 2rem;
    }

    .styles__content {
      order: 1;
      padding: 1.6rem 1.6rem 1.8rem;
    }

    .styles__image {
      order: 2;
      height: 260px;
    }
  }

  @media (max-width: 720px) {
    .styles__grid {
      gap: 1.75rem;
    }

    .styles__card {
      padding: 1.75rem;
      min-height: 0;
    }

    .styles__content {
      padding: 1.5rem 1.5rem 1.7rem;
      gap: 0.75rem;
    }

    .styles__index {
      width: 48px;
      height: 48px;
      font-size: 1.05rem;
      border-radius: 16px;
    }
  }
</style>
